<Page
    x:Class="RuzTermPaper.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <NavigationView
            x:Name="navView"
            Header="Расписание"
            IsSettingsVisible="True"
            IsTabStop="False"
            ItemInvoked="navView_ItemInvoked">
            
            <NavigationView.MenuItems>
                <NavigationViewItem Content="Текущее расписание" Icon="Calendar" Tag="timetable"/>
                <NavigationViewItem Content="Поиск расписания" Icon="Find" Tag="search"/>
                <NavigationViewItem Content="Find" Icon="Find" Tag="NewFind"/>
            </NavigationView.MenuItems>

            <Frame x:Name="contentFrame">
                <Frame.ContentTransitions>
                    <TransitionCollection>
                        <NavigationThemeTransition/>
                    </TransitionCollection>
                </Frame.ContentTransitions>
            </Frame>
        </NavigationView>
    </Grid>
</Page>
