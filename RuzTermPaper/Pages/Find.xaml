<Page
    x:Class="RuzTermPaper.Pages.Find"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RuzTermPaper.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Orientation="Horizontal">
            <TextBox Name="emailBox" PlaceholderText="Email goes here" Height="24" Margin="4" IsSpellCheckEnabled="False"/>
            <Button Margin="4" HorizontalAlignment="Center">
                <StackPanel>
                    <SymbolIcon Symbol="Account"/>
                </StackPanel>
            </Button>
        </StackPanel>

        <ListView Grid.Row="2" HorizontalAlignment="Center">
            <TextBlock HorizontalTextAlignment="Center">
                <Run Text="Current User" FontWeight="Medium"/>
                <LineBreak/>
                <Run Text="1"/>
            </TextBlock >
            <TextBlock Text="2" HorizontalTextAlignment="Center"/>
            <TextBlock Text="3" HorizontalTextAlignment="Center"/>
        </ListView>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Add lecturer" Margin="4"/>
            <Button Content="Add group" Margin="4"/>
        </StackPanel>

    </Grid>
    
</Page>
    